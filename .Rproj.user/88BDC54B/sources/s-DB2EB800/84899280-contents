# Carregue as bibliotecas

library(shiny)

# Defina a interface do usuário
ui <- fluidPage(
    "hello, world!"
)

# Especifica o comportamento do aplicativo definindo uma função server.
server <-  function(input, output, session){
    
}

# Executa o shinyApp
shinyApp(ui, server)

